В этом файле содержатся примеры входного и выходных файлов.

В файле icmp_ipv4.bin содержится единственный ICMP-пакет,
подлежащий инкапсуляции.

В выходных файлах содержатся результирующие пакеты GSE.

Пример запуска:
gse-slice icmp_ipv4.bin -n 3
